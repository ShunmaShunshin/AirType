<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- ============ AirType · 终端仪器 Telemetry Console ============ -->
    <FontFamily x:Key="FontMono">Consolas, Segoe UI</FontFamily>
    <FontFamily x:Key="FontBody">Segoe UI</FontFamily>

    <Color x:Key="ColorBg">#FF0B0E14</Color>
    <Color x:Key="ColorPanel">#FF0E121A</Color>
    <Color x:Key="ColorPanel2">#FF121722</Color>
    <Color x:Key="ColorInset">#FF090C12</Color>
    <Color x:Key="ColorLine">#FF1E2633</Color>
    <Color x:Key="ColorLineSoft">#FF161D28</Color>
    <Color x:Key="ColorText">#FFD8EAE0</Color>
    <Color x:Key="ColorTextDim">#FF86AE94</Color>
    <Color x:Key="ColorTextFaint">#FF52705F</Color>
    <Color x:Key="ColorSignal">#FF5EE07A</Color>
    <Color x:Key="ColorSignalDeep">#FF2FA85C</Color>
    <Color x:Key="ColorAmber">#FFFFB443</Color>
    <Color x:Key="ColorDanger">#FFFF6B6B</Color>
    <Color x:Key="ColorCta">#FF5EE07A</Color>
    <Color x:Key="ColorCtaText">#FF0B0E14</Color>

    <SolidColorBrush x:Key="BrushBg" Color="{StaticResource ColorBg}" />
    <SolidColorBrush x:Key="BrushPanel" Color="{StaticResource ColorPanel}" />
    <SolidColorBrush x:Key="BrushPanel2" Color="{StaticResource ColorPanel2}" />
    <SolidColorBrush x:Key="BrushInset" Color="{StaticResource ColorInset}" />
    <SolidColorBrush x:Key="BrushLine" Color="{StaticResource ColorLine}" />
    <SolidColorBrush x:Key="BrushLineSoft" Color="{StaticResource ColorLineSoft}" />
    <SolidColorBrush x:Key="BrushText" Color="{StaticResource ColorText}" />
    <SolidColorBrush x:Key="BrushTextDim" Color="{StaticResource ColorTextDim}" />
    <SolidColorBrush x:Key="BrushTextFaint" Color="{StaticResource ColorTextFaint}" />
    <SolidColorBrush x:Key="BrushSignal" Color="{StaticResource ColorSignal}" />
    <SolidColorBrush x:Key="BrushSignalDeep" Color="{StaticResource ColorSignalDeep}" />
    <SolidColorBrush x:Key="BrushAmber" Color="{StaticResource ColorAmber}" />
    <SolidColorBrush x:Key="BrushDanger" Color="{StaticResource ColorDanger}" />
    <SolidColorBrush x:Key="BrushCta" Color="{StaticResource ColorCta}" />
    <SolidColorBrush x:Key="BrushCtaText" Color="{StaticResource ColorCtaText}" />

    <!-- 半透明面板（透出背景仪表线稿） -->
    <SolidColorBrush x:Key="BrushPanelGlass" Color="#9E0E121A" />
    <SolidColorBrush x:Key="BrushPanelGlass2" Color="#9E121722" />
    <SolidColorBrush x:Key="BrushInsetGlass" Color="#8F090C12" />

    <!-- ============ 排版（读数=等宽；长文=sans） ============ -->
    <Style x:Key="MonoBig" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{StaticResource BrushSignal}" />
        <Setter Property="FontSize" Value="46" />
        <Setter Property="FontWeight" Value="Bold" />
        <Setter Property="FontFamily" Value="{StaticResource FontMono}" />
    </Style>
    <Style x:Key="H2" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{StaticResource BrushText}" />
        <Setter Property="FontSize" Value="12" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="FontFamily" Value="{StaticResource FontMono}" />
    </Style>
    <Style x:Key="MonoLabel" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{StaticResource BrushTextFaint}" />
        <Setter Property="FontSize" Value="10" />
        <Setter Property="FontFamily" Value="{StaticResource FontMono}" />
        <Setter Property="TextWrapping" Value="NoWrap" />
    </Style>
    <Style x:Key="MonoLabelInk" TargetType="TextBlock" BasedOn="{StaticResource MonoLabel}">
        <Setter Property="Foreground" Value="{StaticResource BrushTextDim}" />
    </Style>
    <Style x:Key="Body" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{StaticResource BrushTextDim}" />
        <Setter Property="FontSize" Value="13" />
        <Setter Property="FontFamily" Value="{StaticResource FontBody}" />
        <Setter Property="TextWrapping" Value="Wrap" />
        <Setter Property="LineHeight" Value="21" />
    </Style>

    <!-- ============ 面板 / 显示井 ============ -->
    <Style x:Key="CardStyle" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource BrushPanel}" />
        <Setter Property="BorderBrush" Value="{StaticResource BrushLine}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="CornerRadius" Value="2" />
        <Setter Property="Padding" Value="16" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
    </Style>
    <Style x:Key="Panel2Card" TargetType="Border" BasedOn="{StaticResource CardStyle}">
        <Setter Property="Background" Value="{StaticResource BrushPanel2}" />
    </Style>
    <Style x:Key="ReadoutWell" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource BrushInset}" />
        <Setter Property="BorderBrush" Value="{StaticResource BrushLine}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="CornerRadius" Value="2" />
        <Setter Property="Padding" Value="16" />
    </Style>

    <!-- ============ 按钮 ============ -->
    <Style x:Key="PrimaryButton" TargetType="Button">
        <Setter Property="Background" Value="{StaticResource BrushCta}" />
        <Setter Property="Foreground" Value="{StaticResource BrushCtaText}" />
        <Setter Property="BorderBrush" Value="{StaticResource BrushCta}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding" Value="18,9" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="FontFamily" Value="{StaticResource FontMono}" />
        <Setter Property="FontWeight" Value="Bold" />
        <Setter Property="FontSize" Value="13" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="bd" CornerRadius="2" Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"
                                          Margin="{TemplateBinding Padding}" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="bd" Property="Background" Value="#FF6FE98A" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="bd" Property="Opacity" Value="0.8" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="bd" Property="Opacity" Value="0.35" />
                        </Trigger>
                        <Trigger Property="IsKeyboardFocused" Value="True">
                            <Setter TargetName="bd" Property="BorderBrush" Value="{StaticResource BrushSignalDeep}" />
                            <Setter TargetName="bd" Property="BorderThickness" Value="2" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="OutlineButton" TargetType="Button">
        <Setter Property="Background" Value="{StaticResource BrushPanel}" />
        <Setter Property="Foreground" Value="{StaticResource BrushSignal}" />
        <Setter Property="BorderBrush" Value="{StaticResource BrushLine}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding" Value="16,8" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="FontFamily" Value="{StaticResource FontMono}" />
        <Setter Property="FontSize" Value="13" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Grid>
                        <Border x:Name="bd" CornerRadius="2" Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"
                                              Margin="{TemplateBinding Padding}" />
                        </Border>
                        <Border x:Name="Glow" CornerRadius="2" BorderBrush="{StaticResource BrushSignal}"
                                BorderThickness="2" Opacity="0" IsHitTestVisible="False" />
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="bd" Property="Background" Value="{StaticResource BrushPanel2}" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="bd" Property="Opacity" Value="0.8" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="bd" Property="Opacity" Value="0.35" />
                            <Setter TargetName="Glow" Property="Opacity" Value="0" />
                        </Trigger>
                        <Trigger Property="IsKeyboardFocused" Value="True">
                            <Setter TargetName="bd" Property="BorderBrush" Value="{StaticResource BrushTextDim}" />
                            <Setter TargetName="bd" Property="BorderThickness" Value="1" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="GhostButton" TargetType="Button">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Foreground" Value="{StaticResource BrushTextFaint}" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="FontFamily" Value="{StaticResource FontMono}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="bd" Background="{TemplateBinding Background}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="bd" Property="Background" Value="{StaticResource BrushPanel2}" />
                            <Setter Property="Foreground" Value="{StaticResource BrushText}" />
                        </Trigger>
                        <Trigger Property="IsKeyboardFocused" Value="True">
                            <Setter TargetName="bd" Property="Background" Value="{StaticResource BrushPanel2}" />
                            <Setter Property="Foreground" Value="{StaticResource BrushText}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="TagBadge" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource BrushPanel2}" />
        <Setter Property="CornerRadius" Value="2" />
        <Setter Property="Padding" Value="8,3" />
    </Style>

</ResourceDictionary>
